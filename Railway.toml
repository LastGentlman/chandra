[build]
builder = "NIXPACKS"
buildCommand = "pip install --upgrade pip && pip install -e ."

[deploy]
startCommand = "python -m streamlit run chandra/scripts/app.py --server.headless true --server.address 0.0.0.0 --server.port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
