[build]
builder = "NIXPACKS"
buildCommand = "pip install --upgrade pip && pip install -e ."

[deploy]
startCommand = "python -m chandra.scripts.run_api"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
